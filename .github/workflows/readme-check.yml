name: README CHECK

#dispara a pipeline somente no pull request
on: 
  pull_request:
    branches:
      - enzo-teste

jobs:
 say-hello:
  name: CHECAR-README
  runs-on: ubuntu-latest

  steps:
   #passo 1: checkout do codigo (opcional, mas boa pratica)
   - name: Checkout code
     uses: actions/checkout@v4

   #passo 2: verificar palavra no readme
   - name: VERIFICAR PALAVRA NO README
     run: grep -q 'super' README.md